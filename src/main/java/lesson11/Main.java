package lesson11;

import java.util.List;
import java.util.Optional;
import java.util.function.BiFunction;
import java.util.function.DoubleUnaryOperator;
import java.util.function.Function;
import java.util.stream.Collectors;
import java.util.stream.Stream;
//import javafx.util.Pair;
import org.apache.commons.lang3.tuple.Pair;

public class Main {
    public static void main(String[] args) {
//        DoubleUnaryOperator operator = a -> (a*0.5);
//        final  double v = operator.applyAsDouble(100);
//        System.out.println("v = " + v);
//        BiFunction<Integer,Float,Number> biFunction = (Integer a,Float b) ->a + b;
//        final Number v = biFunction.apply(10,20.f);
//        System.out.println("v = " + v);
//        new Multiplier() {
//            @Override
//            public Double apply(String a, Double b) {
//                return  Double.valueOf(a) + b;
//            }
//        };
//        final  Multiplier multiplier = new MultiplierImpl(30.);
//
//        List<Boolean> result = Stream.of(1, 2, 3).map(Object::toString).map(id -> id.contains("2")).distinct().collect(Collectors.toList());
//        System.out.println("result = " + result);
//    MyBestClass best1 = new MyBestClass(false);
//    MyBestClass best2 = new MyBestClass(false);
//        Optional<String> id= best1.getId();
//        if (id.isPresent()){
//            System.out.println(id.get());
//        }

    }
}
